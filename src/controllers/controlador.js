const retornarBody = (request, response) => {
    // try {
    //     const body = request.body

    //     return response.status(200).json(body);
    // } catch (error) {
    //     console.log(error);
    //     return response.status(500).json({mensagem:'Erro interno do servidor'});
    // };
};

module.exports = {
    retornarBody
};